<div class="container">
  <div class="row">
  	<h2><%= @category.name %></h2>
  	<% @category.topics.each do |topic| %>
      <%= link_to topic_path(topic) do %>
  	    <%= topic.title %>
  	  <% end %>
  	  <!-- レスポンス数 -->
  	<% end %>
  </div>
</div>