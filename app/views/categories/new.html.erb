<div class="container">
	<div class="row">
		<h2>カテゴリー追加</h2>
		<%= notice %>
		<div class="category_form">
		  <%= form_for(@category) do |f| %>
    	    <%= f.text_field :name, placeholder: "10文字まで" %>
    	  <%= f.submit '追加', class: "btn btn-primary"  %>
          <% end %>
		</div>
	</div>
</div>